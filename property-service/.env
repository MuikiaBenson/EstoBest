# Database connection URI for MongoDB (replace with your actual MongoDB URI)
MONGO_URI=mongodb://localhost:27017/propertydb

# Port on which the server will run
PORT=3001

# JWT secret for token generation and verification
JWT_SECRET=your_jwt_secret_here

# CORS origin for allowing requests (replace with your frontend URL)
CORS_ORIGIN=http://localhost:3000
